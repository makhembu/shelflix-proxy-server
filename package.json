{
  "name": "shelflix-proxy-server",
  "version": "1.0.0",
  "description": "Shelflix stream proxy server (for Render deployment)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "node-fetch": "^2.7.0"
  }
}
